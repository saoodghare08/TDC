export function generateMetadata() {
    return {
        title: 'Get Started | The Diet Cascade',
        robots: { index: false, follow: false },
    };
}

export default function CheckoutLayout({ children }) {
    return children;
}
